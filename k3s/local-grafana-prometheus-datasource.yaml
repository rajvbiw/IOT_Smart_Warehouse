apiVersion: v1
kind: ConfigMap
metadata:
  name: local-grafana-prometheus-datasource
  namespace: default
data:
  prometheus.yaml: |
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        access: proxy
        url: http://local-prometheus:9090
        isDefault: false
